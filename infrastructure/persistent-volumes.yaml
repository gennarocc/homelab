---
# PersistentVolume for app configs
apiVersion: v1
kind: PersistentVolume
metadata:
  name: homelab-configs-pv
  namespace: homelab
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /home/pi/homelab/configs
    type: DirectoryOrCreate

---
# PersistentVolumeClaim for app configs
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homelab-configs-pvc
  namespace: homelab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: homelab-configs-pv

---
# PersistentVolume for databases and monitoring data (local SD card)
apiVersion: v1
kind: PersistentVolume
metadata:
  name: homelab-data-pv
  namespace: homelab
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /home/pi/homelab/data
    type: DirectoryOrCreate

---
# PersistentVolumeClaim for databases and monitoring data
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homelab-data-pvc
  namespace: homelab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-storage
  volumeName: homelab-data-pv

---
# StorageClass for local storage
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-storage
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
